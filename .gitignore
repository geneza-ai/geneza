/bin/
*.test
*.out
# Never commit private keys or issued material.
*.key
!testdata/**/*.key
ca-roots.pem
*.crt
state.db
audit.jsonl
/secrets/

# web control panel build artifacts (source is committed; dist is built by CI/deploy)
web/node_modules/
web/dist/
